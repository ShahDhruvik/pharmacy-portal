// All the endpoints of API are defined here
const enum USER {
  create = '/user/create',
}

export const enum DEF_PATHS {
  auth = '/auth',
  main = '/main',
  common = '/common',
  patient = '/patient',
  practice = '/practice',
  provider = '/provider',
  chat = '/chat',
}


