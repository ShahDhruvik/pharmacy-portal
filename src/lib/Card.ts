// All the Api call that does not concern with the redux store will be defined here and imported wherever needed.
// ex: getAPI,postAPI,etc are defined here
export const numberOfcards = () => {
    return 100
}
